{
  "name": "context-marker-extension",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.26.2",
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "pnpm clean && mkdir -p dist && cp -R public/* dist/ && BUILD_TARGET=options vite build && BUILD_TARGET=background vite build && BUILD_TARGET=content vite build",
    "dev": "pnpm build && (BUILD_TARGET=background vite build --watch & BUILD_TARGET=content vite build --watch & BUILD_TARGET=options vite build --watch & web-ext run -s dist -t chromium)",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "chrome-types": "^0.1.327",
    "happy-dom": "^20.5.1",
    "typescript": "^5.7.0",
    "vite": "^6.1.0",
    "vitest": "^4.0.18",
    "web-ext": "^8.4.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
